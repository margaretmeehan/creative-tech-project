import { BotContext, Storage, ConversationState, UserState } from 'botbuilder';
import { BaseBot } from './BaseBot';
import { StateContext } from './StateContext';
export declare abstract class StateBot<Conversation = any, User = any> extends BaseBot<StateContext<Conversation, User>> {
    conversationState: ConversationState<Conversation>;
    userState: UserState<User>;
    constructor(storage?: Storage);
    getContext(context: BotContext): Promise<StateContext<Conversation, User>>;
}
