import { ConsoleAdapter, Storage } from 'botbuilder';
import { StateBot, StateContext } from './botbldr';
export declare class ConsoleBot<Conversation = any, User = any> extends StateBot<Conversation, User> {
    adapter: ConsoleAdapter;
    constructor(storage?: Storage);
    onRequest(handler: (context: StateContext<Conversation, User>) => Promise<void>): void;
}
