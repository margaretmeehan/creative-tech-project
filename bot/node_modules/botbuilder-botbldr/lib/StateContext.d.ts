import { BotContext, ConversationState, UserState } from 'botbuilder';
export declare class StateContext<Conversation, User> extends BotContext {
    private constructor();
    conversationState: Conversation;
    userState: User;
    static from<Conversation = any, User = any>(context: BotContext, conversationState: ConversationState<Conversation>, userState: UserState<User>): Promise<StateContext<Conversation, User>>;
}
